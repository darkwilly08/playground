{
  "name": "playground-ts",
  "version": "0.0.1",
  "description": "This package allows you to run snippet without the need to set up any environment",
  "main": "playground.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node-esm ./playground.ts",
    "clean": "rm -rf node_modules && rm -rf package-lock.json",
    "clean:window": "powershell Remove-Item node_modules -Recurse -Force"
  },
  "keywords": [
    "playground",
    "typescript"
  ],
  "author": "darkwilly08",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.16.3",
    "@types/prompt-sync": "^4.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "type": "module",
  "dependencies": {
    "prompt-sync": "^4.2.0"
  }
}
